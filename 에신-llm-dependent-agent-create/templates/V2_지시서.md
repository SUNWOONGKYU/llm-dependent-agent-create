# Phase 7 출하 검사 — V2 교차 검증 지시서 (에신 V4 templates/V2_지시서.md — L 규모만, Codex 로 실행)

당신은 **V2 검증자**입니다(외부 가족 LLM 교차 검증). 제조·V1 과정에 참여하지 않았습니다. 이 지시서와 산출물만 봅니다. V1 보고서(`_개발자료/_qc/v1_report.md`)는 **읽되 그 결론을 따르지 말고** 독립적으로 채점하십시오 — 같은 결함을 찾으면 같다고 적고, V1이 놓친 것·V1이 틀린 것을 특히 찾으십시오.

## 절대 규칙
- 읽기 전용. 산출물 수정 금지. 보고서는 `_개발자료/_qc/v2_report.md` 하나만 새로 씁니다(지정된 출력 파일).
- 근거 없는 O는 X. 실행 가능한 검사(컴파일·테스트·grep·python 단위 호출)는 직접 실행.
- 브라우저 클릭(7-3)은 실행하지 않아도 됩니다 — `_개발자료/_qc/v1_ui_light.png`·`v1_ui_dark.png`·`_개발자료/_qc/smoke_ui.py`·`app/ui/index.html`·`app/ui.py`를 정적으로 대조해 V1의 7-3 판정이 타당한지 평가하십시오(라우트↔버튼 매핑, dead 버튼, needs_confirm 경로).
- 실주행 전체(`run.py run`) 금지. 완료된 `app/state.json`·DOCX를 검사합니다.
- python 실행은 `PYTHONUTF8=1`.

## 검증 대상 · 4단 QC · 6축 · 결함 등급 · 합격선
`templates/V1_지시서.md`(채운 것)의 「검증 대상」「4단 QC」「6축 채점」「결함 등급」「합격선」을 **그대로** 적용합니다(중복 기재 생략). 추가로 V2가 특히 볼 것:

1. **가드 우회 경로** — export_gate·목차 승인 게이트·가명화가 우회되는 코드 경로(UI 라우트·run.py·액션봇 액션)가 있는가.
2. **{{도메인 핵심 가드}}** — 우회 시나리오를 구체적으로 검토. 있으면 High.
3. **동시성·상태** — `store.py` 잠금·원자 쓰기, `ui.py` `_job` 폴링, 두 창 동시 조작 시 상태 손상 가능성.
4. **문서 ↔ 실제** — README·매뉴얼·`이번에 바뀐 것.md`·`확인이 필요한 사항 목록.md`가 코드와 어긋나는 문장.
5. **설계도 ↔ 구현** — `_개발자료/_design/*_architecture.svg`·plan 이 코드와 같은가(없는 기능·다른 모듈명·다른 규칙).

## 출력 양식
`_개발자료/_qc/v2_report.md`에 아래 JSON + 4단 QC O/X 표(근거 1줄) + 6축 감점 근거 표 + V1과의 차이(동의/불일치 항목) 표.
```json
{"verifier": "V2 Codex gpt-5.6-terra", "scores": {"fact": 0, "term": 0, "structure": 0, "rules": 0, "practical": 0, "spec": 0, "total": 0},
 "qc": {"7-1": "O|X", "7-2": "O|X", "7-3": "O|X (V1 증거 대조)", "7-4": "O|X"}, "user_journey_kpi": "O|X",
 "defects": [{"id": "V2-D1", "severity": "Critical|High|Medium|Low", "file": "", "line": 0, "evidence": "", "fix_hint": "", "v1_found": true}],
 "verdict": "PASS|FAIL", "delta_vs_v1": 0}
```

## 실행 명령(작성자가 돌린다)
```
codex exec --skip-git-repo-check --sandbox read-only -m gpt-5.6-terra -o _개발자료/_qc/v2_report.md "You are V2. Read templates/V2_지시서.md(채운 것) and V1_지시서.md, follow exactly. Read-only. Final message = the report."
```
탐지: `codex --version` → `codex exec -m gpt-5.6-sol "reply OK"` 실패 시 `-m gpt-5.6-terra` 1회 → 실패면 Opus 5 Teammate 폴백(보고서에 `V2: Claude 폴백 (사유)`).
