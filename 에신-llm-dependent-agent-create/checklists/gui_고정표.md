# 화면 구성 고정표 (checklists/gui_고정표.md · 에신 V4) — GUI 트랙 V1 대조 기준. 원본은 docs/31 §화면 구성 고정표. 어휘(접수·작업·사건)는 자리 표시 — 도메인 어휘로 바뀐 것은 O.

| 기둥 | 순서 | 블록 | 접기 | 안에 들어가는 것 | 자동 펼침 |
|---|:-:|---|:-:|---|---|
| 좌 | 0 | 연결 끊김 띠 `#status-alert` + **[상태 새로 고침]** | — | 끊기면 띠 한 줄 + 좌측 블록 흐림(`.unverified`). 다른 블록을 펼치지 않는다 | — |
| 좌 | 1 | **페르소나** — "무엇을 하는가" | 접기(기본 닫힘) | 하는 일 1줄 · 근거 1줄 · 접이식 「판단 및 작성 방법」 3줄. **"하지 않는 일" 목록은 여기 두지 않는다**(우측 안전 체계와 중복) · 목표도 여기 두지 않는다 | — |
| 좌 | 2 | **LLM** — "판단·작성·검증을 맡는다" | **고정(접지 않음)** | **세 자리만**: 판단·작성 `Claude Code CLI` / 1차 검증 `Claude Code CLI`(따로 부름) / 2차 검증 `Codex CLI`(다른 회사 AI). 칩 = 준비됨 / 확인하는 중… / 확인 못 함 / 없음. 바닥에 "※ 구독제 사용 · API 키 미사용". **다른 CLI 를 4번째 자리로 넣지 않는다** | — |
| 좌 | 3 | **도구** — "읽고 계산하는 도구" | 접기(기본 닫힘) | **맨 위 = 자동 계산(코드, AI 미개입) 「내장」** + 그 항목 나열 · 안내 1줄(없으면 사람이 무엇을 대신하나) · 설치 도구 칩 목록 | 도구 하나라도 「없음」이면 펼친다 |
| 좌 | 4 | **지식베이스** — "지식이 들어오는 4대 경로" | 접기(기본 닫힘) | ① 직접 주입(규칙 skills/*.md) · **사용자 기준 입력칸**(규칙이 정하지 않은 값 — 값+근거+[저장], 비우면 「확인 필요」) · ② RAG(투입 자료) · ③ API · ④ MCP · 작업 지식(누적). **좌측에서 값을 적는 유일한 자리** | 필수 기준이 비어 있으면 펼친다 |
| 중 | 0 | `ws-head` "Workspace (작업공간)" + 안내 캡션 | — | 그 아래 `#flash`, LLM 부재 경보 `#brain-alert`(+[설치 도우미 열기]), 접이식 설치 도우미, **작업 게이트**("오른쪽 작업 목록에서 고르거나 [+ 새 접수]") | — |
| 중 | 1…N | **단계 카드** `section.block` 「(번호) 단계명」 | — | 도메인 교체 구역. 번호·이름은 우측 「진행 단계」 목록과 **1:1 동일** | — |
| 우 | 0 | 도움 띠 `help-strip`: **[사용자 매뉴얼] [설치 도우미]** | — | 매뉴얼은 새 탭 | — |
| 우 | 1 | **설정** — 산출물에 찍히는 값(학교·사무소·프로필) "한 번만" | 접기(기본 닫힘) | 폼 + [저장]. 비운 칸은 산출물에도 빈칸 | — |
| 우 | 2 | **작업 목록** | **고정** | 건수 배지 · **[+ 새 ○○ 접수]**(누르는 곳은 여기) · 진행 중 목록 · 「완료」는 접힘(건수만, 검색은 완료 안에만) · 작업 재활용 | — |
| 우 | 3 | **목표** — "이 ○○에서 무엇을 하나" | 접기(기본 닫힘) | 목표 1줄(`goal-fixed`) + 완료 기준 1줄. **목표는 여기에만** | — |
| 우 | 4 | **진행 단계 (자율 루프)** | **고정** | `ol.steps` 단계 목록(가운데 카드와 1:1), 「기록에서 되살림」 배지, 기록 없는 단계는 회색 「기록 없음」 | — |
| 우 | 5 | **안전 체계** — "경계와 가드레일" | 접기(기본 닫힘) | 줄마다 「라벨 · 한 줄」 + 접이식 「왜」: 보관(이 PC 안에만) · AI 전송(무엇을 가리나) · 도메인 가드 n줄 · 확정 권한(자동 제출·확정 없음) · 멈추기 기준(다시쓰기 N회·무응답 M분) · 자동 백업 · 숨은 지시. 상태 칩 없음(칩은 설명 반복). **안전 체계는 여기에만 — 좌측에 설정·안전 항목 금지** | 연결 끊김 때도 펼치지 않는다(제목 옆 배지만) |
| 우 | 6 | **오늘의 작업 로그** — "지난 기록" | 접기(기본 닫힘) | 작업 필터 · 행위자 구분(사용자/시스템/**AI**) · 「로그가 남는 방식」 · [로그 폴더 열기] | — |
| 우 | 7 | **기한·대기 관리** | 접기(기본 닫힘) | 도메인의 기한·대기 항목(예: 보존기간·시효 / 확인 대기 인용 등). 없으면 「해당 없음」 1줄 | 임박 항목 있으면 배지 |
| 하단 | — | `footer-line` 유의사항 · 기록 · **끝낼 때(검은 창 닫기)** · 접이식 고지사항(오픈소스 라이선스) | — | 유의사항은 여기로 내린다(헤더·기둥 사이에 띠 금지) | — |
| 떠 있음 | — | **액션봇** FAB「AI 대화」+ 패널 | — | FAB 는 몸통 드래그, 패널은 머리만 드래그. 경고 띠 + 로그 + 입력. 실행형 기본(needs_confirm) | — |

