# 설계 산출물 규격 — 목표서 삽입 절 · plan · 관계도/흐름도 · BOM · 설계 검증 · 갱신 규정

> 에신 V4 docs 모듈 — Phase 2 · 4 · 5 · 6 마감 에서 읽는다. 목적: 설계 파일 5종의 양식과 "구현이 달라지면 아키텍처 파일을 고친다"는 규정
> 문서 순서: Phase 1 설계서 초안+관계도·흐름도 → Phase 2 목표서 승인 → Phase 3·4 부품 발굴·검증(BOM) → **Phase 5 설계서 확정+설계 검증** → 6 조립 → 7 출하 검증 → 8 출하 → 9 운영. **부품(BOM)이 정해져야 설계서(plan.md)가 완성된다** — Phase 4 없이 Phase 5로 못 간다. 설계서(plan.md) = *어떻게 만드는가*, 목표서(mbo 목표서) = *무엇을 왜 + 승인* — 역할이 다르고 이름도 바꾸지 않는다.
> 출처: 에신 V3.27 본문(2026-09-17)을 절 단위로 옮긴 것. 절차·표는 그대로, 판정 권한은 SKILL.md §3 Phase 표가 단일 출처.

## Phase 2. 목표 정의 — mbo-천상 호출 + 승인 게이트 (★V3.22 — 양식 복제 폐지, 스킬 호출로 전환)

**목표서 양식·승인·MBO 파일 저장·결과 보고는 `mbo-천상` 스킬이 단일 출처다.** 에신은 양식을 베끼지 않고 `mbo-천상`을 호출해 그 절차를 그대로 탄다. 에신이 보태는 것은 딱 하나 — 에이전트 고유 「제조 대상 — 7대 구성요소 + 메타」 절을 목표서에 **삽입**하는 것뿐이다.

이 단계에서 에이전트 **규모(S/M/L)를 확정**한다. 배치/포털 모드는 자동으로 L (배포형·다수 에이전트·호스팅) 판정.

### 호출 규격 (에신 → mbo-천상)

| mbo-천상 단계 | 에신에서의 처리 |
|---|---|
| PHASE 1 Step 1 맥락 분석 · Step 2 스무고개 | **건너뜀** — 에신 Phase 1(10라운드 + α 문답)이 이미 수행. Phase 1 산출물(기획안·출력 대상 명세)을 mbo-천상 입력으로 넘긴다 |
| Step 3 AS-IS 조사 | 수행 — 현재 사람이 하는 방식(수작업)을 조사해 「현재 vs 목표」의 AS-IS 칸을 채운다 |
| Step 4 MBO 목표서 작성 | mbo-천상 양식(최상위 목표·현재 vs 목표·KPI·산출물·완료·검증 기준[공개본 명칭 「목표 달성 평가·검증 기준」+「자가수정 한도」]·실행 계획·리스크) **그대로** + 「최상위 목표」 바로 아래에 아래 **삽입 절**을 넣는다. 제목은 `MBO 목표서 — 에이전트 제조: {에이전트명}` |
| Step 5 PO 승인 | mbo-천상 게이트 그대로. ⛔ 승인 없이 Phase 2.5 이후 진행 절대 금지. 수정 요청 시 재제시 |
| PHASE 2 MBO 파일 저장(공개본 명칭 「목표서 파일 저장」) | **에신 작업 폴더**(Phase 0에서 지정) 루트에 `YYYY_MM_DD__HH.MM_mbo-천상_{에이전트명}.md` 저장 — 컴팩션·세션 교체 대비 |
| PHASE 2 자율 실행 | 에신 Phase 2.5 → 9 가 곧 실행 계획. 실행 계획 표의 Task = 에신 Phase 목록 |
| PHASE 3 결과 보고 | 에신 Phase 8 출하 보고서 직후 `/mbo-천상 report`(공개 설치본 `/mbo report`) 로 KPI 실측·산출물·검증 대조 → 저장한 MBO 파일 하단에 덧붙임. 자가수정 게이트(미달 시 Phase 7 QC 또는 Phase 2 재승인 회귀)도 mbo-천상 규정 따름 |

KPI 작성 시 에신 고유 제약(mbo-천상 양식 위에 얹음):
- 반드시 **사용자 여정 KPI 1개 이상** — "파일 생성"은 데이터 KPI일 뿐
- 비용/보안/컴플라이언스 KPI 각 1개 이상 권장 (V3.0)
- 실행 계획 표에는 에신 Phase 2.5·3·4·5·6·7·8·9 를 Task로 나열 (규모별 생략 Phase는 "생략 — 사유" 명시)

### 삽입 절 — 목표서 「최상위 목표」 바로 아래 (에신 고유)

```markdown
## 제조 대상 — 7대 구성요소 + 메타 (★V3.22 정렬 — 가이드 정의 순서)
{Phase 1 기획안 요약}
🏷️ 이름       : {에이전트 부르는 이름 — 한글·영문 자유}
─── 7대 구성요소 ───
🎭 ① 페르소나  : {누구로서 어떻게 일하는가 — 정체성·어조·판단 기준. system_prompt에 박힘} ★V3.22
🎯 ② 목표      : {한 줄 — 달성하려는 것 + 완료 기준}
🤖 ③ LLM       : 1순위 {모델명} → 폴백 {순서} ★R11 — Phase 6 llm_providers.py에 박힘 (구독 CLI면 "CLI: {목록}, API 0")
📘 ④ 지식베이스 : {직접 주입 / RAG / API / MCP 중 사용 경로} + 작업 지식(메모리) {1회 / 누적 / 중복 방지 등}
🔧 ⑤ 도구      : {호출할 외부 도구 목록 또는 "없음"}
🛡️ ⑥ 안전 체계 : ★V3.22 — 아래 4항을 이 요소 아래 묶는다
   🛑 종료      : {최대 N회 시도 / 비용 X원 초과 시 / 자가평가 N점 이상 시 / 표준값} ★R12
   🔐 보안      : {개인정보 처리 yes·no / 데이터 보존 기간 / 시크릿 .env 의무} ★R12
   💰 비용·성능 : 1회 한도 {N원} · 월 한도 {N원} · P95 {N초} ★R12 — BOM 🔵 비용 가드에 박힘 (구독 CLI면 호출 횟수·시간 상한으로 대체)
   🚫 도메인 가드: {이 에이전트 특유 위험 차단 — 예: 가짜 인용·데이터 날조·비가역 작업 fail-closed / "없음"}
🔁 ⑦ 자율 루프 : {1회형 / 폴링형 / 사용자 응답형} + 사람 개입 지점 {어디서 승인 받나} + 루프 내부 자가평가 {있음·기준 / 없음}
─── 메타 ───
📂 분류       : {유형 — 수집/변환/분류/큐레이션/생성/분석/대화/실행/하이브리드}
🖥️ 사용자 층   : {개발자·엔지니어 / 일반인·전문직 실무자 / 내부 도구} ★V3.9 — ②·④ 답변이면 Phase 6 로컬 GUI 필수 분기 + Phase 7 `-HumanFacing` 전달 근거
제조 모드    : 단일 / 배치(N개) / 포털(N개 + 플랫폼)
발굴 경로    : A 지정 원자재 {경로} / B 공개 발굴  ★V3.25
구동 방식    : {웹사이트형(서버에 올려 다수가 사용, Supabase 등) / 데스크톱형(로컬 PC 설치, 혼자 쓰거나 여러 사람이 공용 사용)}  ★V4.4 — 구 "인프라 A/B" 필드와 통합
에이전트 규모 : {S / M / L}
운영 OS      : Windows / macOS / Linux / Cloud
```

⛔ **PO 승인 없이 Phase 2.5 이후 진행 절대 금지.** PO가 수정 요청 시 목표서를 고쳐 재제시.

---

## Phase 4. 분해·검증 — "정밀 선별" ★공장의 심장

> ★V3.12 부품을 채택하면 **회귀 테스트 승계표**(원본이 보장하던 행동 → 원본 테스트 → 우리 대응 테스트 → 실호출 증거)를 함께 만든다. 코드만 가져오고 검증을 두고 오면 안 된다.

**발굴물을 통째로 신뢰하지 않는다.** 작업대에 올려 부품 단위로 검증하고, 결함은 걸러낸 뒤 검증된 부품만 선별한다.

### 4-1. 입고 검사 — 후보별 5대 KPI 채점

| 지표 | 합격선 | 측정 |
|------|--------|------|
| **명료성** | Read 1회 5분 내 이해 | 사용 시점·절차·예시 존재 |
| **운용 충돌** | **0건 — 타협 불가** | 한국어 출력·보안 가드·SAL/MBO 자동감지 위반 0 |
| **최신성** | 6개월 이내 업데이트 | 커밋·릴리스·작성 날짜 |
| **의존성 부담** | npm/pip 추가 1개 이하 또는 검증된 메이저 OSS | 의존성 트리 |
| **검증 가능성** | 결과물 객관 검증 가능 | V1 투입 가능 여부 |

5개 중 4개 미만 → 후보 폐기 또는 fork. **"운용 충돌 0건"은 어떤 경우에도 타협 불가.**

### 4-2. 부품 단위 분해

후보를 통째로 채택하지 않는다. **함수·모듈·프롬프트 블록·설정 단위로 쪼개** 각각을 분류한다.

### 4-3. 안전성 진단 (주작 방식) → 걸러내기

발굴물을 위험도·안전성 관점에서 감사 (주작-sal-da 방식). 다음은 발견 즉시 제거:
- `eval()`·`exec()`·검증 없는 shell 직접 실행
- 하드코딩된 시크릿·API 키·절대경로
- PO 보안 가드 위반 (외부 드라이브 무단 접근, `.env` 노출 등)
- deprecated 라이브러리·API 의존
- 영어 강제 출력 등 한국어 운용 규칙 위반
- `href` 없는 버튼·죽은 링크 (UI 에이전트의 경우)
- 1년 이상 미유지보수 코드

### 4-4. BOM 산출 — 5분류 (V3.0 보강) ★

```
📦 BOM — Bill of Materials (선별 부품 목록)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[🟢 정상 — 그대로 적용]
  • {부품명} ← {출처}:{경로} | 용도: ___

[🟡 재단 후 적용]
  • {부품명} ← {출처} | 재단 내용: ___

[🔴 걸러냄 — 적용 거부]
  • {부품명} ← {출처} | 사유: {보안위반/deprecated/충돌}

[🏭 자체 제작 필요]
  • {부품명} | 사유: 발굴물에 검증된 대체재 없음

[🔵 공통부 — N 에이전트가 공유] ★V3.0 신설
  • {부품명} | 적용 범위: {몇 개 에이전트} | 횡단 관심사: {비용·세션·도구·면책·로깅·피드백}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 4-5. 이중 분류 규칙 (V3.1 신설 — 🔵 ∩ 🏭 교집합 처리)

한 부품이 여러 분류에 동시 해당할 수 있다. 다음 우선순위 규칙으로 단일 레이블 부여:

```
🟢/🟡  >  🔵  >  🏭  >  🔴
```

**판정 절차**:
1. **출처 우선** — 발굴물에서 가져온 것이면 🟢/🟡 (재단 여부로 갈림). 출처가 외부면 무조건 🟢/🟡 분류가 1순위.
2. **범위 우선** — 자체 제작이지만 N 에이전트가 공유한다면 🔵 (배치/포털 모드만). 단일 모드에서는 🔵 불가.
3. **신규 제작 단독 사용** — 🏭
4. **결함** — 🔴 (다른 분류와 동시 적용 불가, 항상 거부)

**예시**:
- 비용 가드 코드를 자체 제작했고 93 에이전트가 모두 쓴다 → **🔵** (🏭 아님)
- 비용 가드를 PyPI 라이브러리로 가져와 그대로 적용 → **🟢** (🔵 아님)
- 단일 에이전트 전용 OCR 코드를 자체 제작 → **🏭** (🔵 아님)
- 단일 에이전트가 OSS OCR 라이브러리 가져와 한국어 폰트만 추가 → **🟡** (🏭 아님)

BOM 작성자는 한 부품을 두 분류에 동시 기입하지 않는다 — 단일 레이블만.

🔵 공통부 부품의 예 (제작자 선행 프로젝트 93개 + claude-news-agent 사례):
- 비용 가드 3중 (per_run / per_user_monthly / monthly)
- 세션 HMAC + role 기반 권한 + 회원가입 승인
- 공급자 공통 tool round-trip (Anthropic/OpenAI/Gemini/xAI 중 **채택한 것**만 실호출 — ★V3.12)
- **LLM 폴백 체인 모듈 ★V3.5 신설 표준 자산** — 단일 키 401/402/429/credit 한도 시 자동 폴백 (예: `llm_providers.py` Claude→Gemini→GPT→Grok). (회고 사유: claude-news-agent 첫 실행 시 ANTHROPIC_API_KEY 401로 전체 멈춤 — 폴백 추가 후 정상 작동) ⚠️ **이 API-키 폴백 체인은 웹사이트형(포털·서버) 런타임의 표준이다.** 데스크톱형(PO 본인용 로컬)은 위 💳 원칙대로 **`claude` CLI(구독) → `codex` CLI 2차 검증**을 표준으로 하고 API-키 폴백 체인은 *배포용일 때만* 넣는다. (제작·검증은 웹사이트형/데스크톱형 불문 항상 구독 — API 금지.)
- 면책·디스클레이머 정책
- 첨부 처리 (Vercel 4.5MB → Storage signed URL 우회)
- 사용 로그·피드백·관리자 콘솔

배치/포털 모드는 🔵가 반드시 채워진다 — 비어 있다면 Phase 1c를 다시 점검한다.

**단일 모드 예외 (V3.5)**: 단일 모드는 원래 🔵 불가지만 **LLM 폴백 체인 모듈**(웹사이트형) 또는 **CLI 호출 헬퍼 `_sub_env()`+claude·codex CLI**(데스크톱형)만은 단일 모드에서도 🏭(자체 제작)로 분류하여 반드시 포함한다. 단일 LLM 의존은 첫 출하 직후 멈춤 사고를 부른다.

---

## Phase 5. 설계서 확정 + 설계 검증 (★V4.4 — 작성자 자체 채점 폐지 → 별도 검증자 이진 판정)

### ★ 자기완결 원칙 폐지 (★V3.22 — 두 공장 동기화, V4.4 에서도 유지)

V3.4~V3.21의 "자기완결 원칙"(외부 스킬 호출 금지·외부 AI 호출 금지·내장 Subagent만 검증)은 **폐지**한다. 대신:
1. **핵심 절차는 계속 본문에 내장** — Phase 5 설계 검증 체크리스트, Phase 7 QC 4단은 그대로 이 문서 안에 있다. `/review-evaluate`·`/pro-persona-debate`·`/주작-sal-da` 호출 의무는 여전히 없다.
2. **외부 연결은 정확히 2개(의무)** — 목표 관리 `mbo-천상`(Phase 0-0에서 보장, 필수) · 검증 V2 `Codex`(「검증 편제」 절, 폴백 있음). 이 둘 외 외부 **의무** 호출 없음. `/diagram-기본`·`/5times-debug-loop` 같은 **선택 보조 스킬**은 있으면 쓰고 없으면 본문에 적힌 내장 폴백으로 대체한다(공개 사용자 기준).
3. **검증 주체 = V1·V2** — 「검증 편제 — 작성자 · V1 · V2」 절이 단일 출처. ★V4.4 — Phase 5(설계 검증)·Phase 7(출하 검증) **두 곳** 각 1회. V3.23~V4.3 시기의 "검증은 Phase 7 뿐"은 폐지 — 설계 단계에서 문서만 보는 별도 검증을 추가해 조립 이후 구조적 재작업을 줄인다(시간 상쇄: 5A 100점 루브릭·5B 진단·해체 절차를 걷어낸 만큼 Phase 5 설계 검증에 쓴다).

이 폐지·개정은 형제 공장 `/skill-create`에도 동일 적용된다(같은 날 짝 개정).

**Phase 5는 순서가 있다 — ① 설계서 확정 → ② V1 설계 검증(문서만) → ③ 반려 항목만 이진 재확인.** 규모(S/M/L)에 따라 절차가 갈리지 않는다 — 어느 규모든 같은 12항목.

---

### 5-1. 설계서 확정

Phase 4 BOM(5분류)이 정해진 뒤에야 plan.md 를 확정할 수 있다 — **부품이 정해져야 설계서가 완성된다.** 작성자가 다음을 한 번에 정리한다:
- Phase 1 초안 plan.md 에 Phase 3·4 에서 나온 부품 배치(어느 구성요소에 어느 BOM 부품이 들어가는지)를 반영해 **확정본**으로 고친다.
- 관계도·흐름도 SVG 를 확정본 plan.md 와 절 번호 1:1 로 맞춘다.
- Phase 1 기획안 KPI·Phase 2 MBO 측정 지표가 산출물에 반영됐는지, 글로벌 헌법(`~/.claude/CLAUDE.md`) 위반이 없는지 스스로 훑는다(V1 에게 넘기기 전 자기 정리 — 채점은 아니다).

### 5-2. V1 설계 검증 — 이진 체크리스트 12항목 (문서만, 코드 미실행)

V1(Claude Code Teammate·Sonnet 5·제조 미참여, Phase 7 V1 과 다른 세션)을 `templates/V1_지시서_설계검증.md` 로 소환한다. **입력은 plan.md·관계도/흐름도 SVG·bom.md 뿐 — 코드·app/ 폴더는 읽지 않는다.** 작성자는 상태 변경 없는 준비 작업(폴더 뼈대 복사 등)을 검증 중 병행할 수 있다.

★ **이 절(5-2)이 12항목 정의·판정 방법의 단일 출처다** — SKILL.md §5 는 같은 12항목을 요약 나열한 것뿐이고, `templates/V1_지시서_설계검증.md`·`checklists/phase5_설계검증.md` 는 이 절을 가리킨다. 세 곳 항목 문구가 갈리면 여기가 맞다.

12항목(각 O=소거됨/X=잔존, 배점 없음):
1. 7대 구성요소가 plan.md 에 전부 명시됐는가
2. 관계도에 7대 구성요소가 전부 노드로 존재하는가
3. 흐름도가 시작→종료까지 끊긴 경로 없이 연결되는가
4. 관계도에 붕 뜬(어느 화살표에도 연결 안 된) 구성요소가 없는가
5. plan.md 각 항목이 목표서(mbo) 승인 내용과 일치하는가
6. BOM 에 🔴(걸러냄) 부품이 남아있지 않은가
7. BOM 부품에 라이선스 미상·금지 라이선스가 없는가
8. plan.md 에 실패 모드(에러·한계 상황) 정의가 있는가
9. BOM 각 부품이 7대 구성요소 중 최소 하나에 매핑되는가
10. 트랙(웹사이트형/데스크톱형)·규모(S/M/L) 결정이 plan §1 좌표에 명시됐는가
11. 관계도·흐름도 SVG 가 plan.md 최신 버전과 절 번호 1:1 로 대응하는가
12. plan.md 12항목 문답에 빈칸이 없는가

#### 판정 방법 (문서만 보고 O/X 를 낼 수 있게 — V1 필독)

- **① 항목 3(흐름 끊김)** — 관계도의 시작 노드에서 화살표만 따라가 종료 노드까지 한붓그리기로 도달 가능한지 눈으로 추적한다. 종료 노드가 아닌 노드는 입력 화살표 1개 이상·출력 화살표 1개 이상을 가져야 한다. 어느 쪽이든 0개면 X.
- **② 항목 5(목표서 대조 범위)** — plan.md §1 좌표·§2 7대 구성요소·§3 메타의 핵심값(목표 한 줄·LLM 1순위+폴백·비용/성능 한도·에이전트 규모)이 MBO 목표서 「제조 대상」 삽입 절과 다른 값을 말하면 X. 문장 표현이 달라도 값이 같으면 O(토씨 대조 아님).
- **③ 항목 6(BOM 🔴 보존 정책)** — 🔴 로 분류된 부품이 `bom.md` 의 `[🔴 걸러냄]` 섹션에 사유와 함께 남아 있는 것은 정상(감사 기록)이다. X 판정 대상은 그 부품이 **🟢/🟡/🏭 로 재분류되지 않은 채 실제 채택 목록(🟢/🟡/🏭/🔵)에 다시 섞여 있는 경우**뿐이다.
- **④ 항목 7(금지 라이선스 목록·판정 근거)** — 금지 판정: 상용/재배포 명시적 금지 라이선스(예: CC BY-NC, 비상업 전용 커스텀 EULA), LICENSE 없고 출처도 불명, 또는 `bom.md` 「라이선스·확인일」 칸이 빈칸이거나 "확인 불가"로 적힌 경우. 그 외(MIT·Apache-2.0·BSD 등 통상 허용 라이선스 + 확인일 기재)는 O.
- **⑤ 항목 11(SVG↔plan 절 1:1 매핑 방법)** — SVG 관계도/흐름도의 각 박스·화살표 라벨에 적힌 절 번호(§)가 plan.md 목차의 절 번호·제목과 같은 순서로 대응하는지 대조한다. 노드에 절 번호가 없으면 노드 제목 문자열이 plan.md 소제목과 일치하는지로 대체 판정한다.
- **⑥ 항목 12(plan 내 12항목 문답 위치)** — 여기서 "12항목"은 Phase 1 「발굴 항목 12개」(이름·분류·목표·루프·도구·지식베이스·LLM·종료·보안·비용·사용자 층·페르소나, `docs/10` §발굴 항목 12개 표)를 가리킨다 — Phase 5 자체의 이 이진 체크리스트 12항목과는 다른 목록이다. 위치는 `templates/양식/plan.md` §1 좌표 표·§2 7대 구성요소 절·§3 메타 줄. `{{ }}` 가 하나라도 남아 있으면 X.

결과는 `_개발자료/_design/phase5_design_verify.md` 에 12항목 O/X 표 + X 항목 근거로 저장한다.

### 5-3. 반려 처리 — 그 항목만 이진 재확인

X 가 있으면 작성자가 **그 항목만** 고치고 V1 에게 그 항목만 다시 확인시킨다("소거됨/잔존"). 12항목 전체 재검토·재라운드 없음. 전항 O 가 되어야 Phase 6 진입.

### Phase 5 외부 연결 (★V3.22 — V3.4 배제 조항 대체)

✅ `mbo-천상` — 목표서·승인·기록 (Phase 0-0 보장, 필수)
✅ V1 Claude Teammate — 설계 검증 (Phase 5 1회)
✅ V2 Codex — 외부 가족 LLM 교차 검증 (Phase 7 1회, 미설치 시 Opus 5 Teammate 폴백)
✅ Claude Code 내장 `Agent` — V1(Phase 5·Phase 7)·V2 폴백 소환
❌ 그 외 외부 스킬(`/pro-persona-debate`·`/주작-sal-da`·`/review-evaluate`) 의무 호출 없음 — 절차는 본문 내장

### Phase 5 결과 → Phase 6 진입

- 12항목 전항 O → Phase 6 즉시 진행. 제품 내부 자가채점은 여전히 출하 판정 권한이 없다(SKILL.md §1 원칙 2).

---

### ★V4.4 아키텍처 파일 갱신 규정 — 만들면서 고치고, Phase 6 마감에 확인한다

Phase 5 에서 확정한 관계도·흐름도(`_개발자료/_design/{이름}_architecture.svg`)와 `plan.md` 는 **구현하면서 반드시 달라진다**(모듈 이름·판정 규칙·안 만든 기능·재시도 횟수·명령 이름). **구현이 설계와 달라지는 그 시점에**(모듈을 바꾸거나 기능을 빼거나 규칙을 바꿀 때) 작성자가 SVG·plan 을 같이 고치고, Phase 6 을 끝내기 전에 한 번 더 전체를 대조해 **구현 기준으로 SVG·plan 을 확정한다** — 화면 단계 카드 ↔ 흐름도 번호가 1:1 로 맞아야 하고, 흐름도에 있는데 안 만든 것(예: "표절 유사도")은 지운다. 갱신하지 않은 아키텍처 파일은 "없는 기능을 있다고 읽게 하는" 결함이다(2026-09-17 학위논문 에이전트: 흐름도 5곳이 구현과 달랐음). Phase 7 V1 「사실 정확성」이 SVG 도 대조하되, **설계 자체의 적합성은 Phase 5 에서 이미 끝났으므로** Phase 7 은 "구현 ↔ 이 시점의 SVG/plan" 대조에 집중한다.

---
