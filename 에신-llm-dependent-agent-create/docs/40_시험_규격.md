# 시험 규격 (docs/40 · 에신 V4)

> Phase 6 조립 중 같이 만들고, Phase 6→6b 게이트·Phase 7-2 에서 돌린다. 원천: 완성도 높은 실물 에이전트가 시험 1,194건·계약 시험·돌연변이 검사로 품질을 지킨 것 / 오늘 제조본은 16건뿐이었던 것.

## 1. 최소 묶음 (templates/test_gates.py 가 뼈대)
| 묶음 | 무엇을 재나 | 최소 건수 |
|---|---|:-:|
| 진입점·패키징 | `시작.bat` ASCII·CRLF·BOM 없음·echo 괄호 없음 · 전 모듈 컴파일 · 시크릿 패턴 0 · 127.0.0.1 만 | 5 |
| LLM 세 자리 | `order == ["claude","codex"]` · 자식 프로세스 env 에 `*_API_KEY` 없음 | 2 |
| 화면 계약 | `uicontract` 표의 렌더러·element 가 index.html 에 있음 · status/state 키가 전부 계약에 있음 | 2 |
| 가드 | plan §안전 체계 항목마다 1개 이상(가명화 왕복·누출 스캔·출력 필터·needs_confirm·초기화 확인+백업·상한) | 6+ |
| 도메인 결정론 | 코드로 계산하는 값마다 1개(입력→기대값) | 도구 수 |
| 상태·재개 | 단계 스냅샷 생성 · 이어쓰기(중단 후 재개가 초고를 다시 만들지 않음) | 2 |
| **정의됨 ≠ 호출됨** | bom·대조표에 `module.func` 로 적힌 함수가 존재하고 **다른 곳에서 불리는가**(test_documented_function_exists_and_is_called) · 2차 검증(Codex)이 파이프라인에서 실제로 불리는가(test_second_verifier_is_wired) | 문서 함수 수 + 1 |
| 돌연변이 자가 검사 | 새 시험을 넣기 전 일부러 망가뜨려 빨간불 확인(기록만) | 1 |

## 2. 규모별 하한
| 규모 | 시험 건수 | 비고 |
|---|:-:|---|
| S | 20 | 최소 묶음 전부 |
| M | 60 | + 도메인 결정론 전수 + 라우트별 1개 |
| L | 150 | + 시운전 회귀 |

GUI 트랙은 규모와 무관하게 `templates/smoke_ui.py` 를 `_개발자료/_qc/smoke_ui.py` 로 복사해 채우고(우측 블록 어휘 7개), Phase 6→6b 게이트와 Phase 7-3 에서 돌린다(고정표 자기 대조 + 클릭 + 콘솔 0 + 캡처).

## 3. 금지
- **글자가 근처에 있는가 시험** — 소스를 고정 글자 수로 잘라 낱말을 찾는 시험은 호출을 지워도 통과한다. 함수 본문(중괄호 짝)·라우트 경계로 자른다. 정규식 역슬래시는 `chr(92)` 로.
- **실 state 에 쓰는 시험** — `monkeypatch` 로 `store.STATE_PATH`·`BACKUP_DIR` 를 임시 폴더로 돌린다.
- **미확인을 통과로** — probe 의 `found=None` 을 False/True 로 단정하는 시험 금지.

## 3b. Windows 어댑터 검사
`templates/verify_windows_adapter.ps1` 을 `_개발자료/tools/` 로 복사(UTF-8 **BOM 포함** 유지 — 없으면 PS 5.1 한글 로캘에서 조용히 죽는다). Phase 6→6b 게이트에서 `powershell -ExecutionPolicy Bypass -File _개발자료/tools/verify_windows_adapter.ps1 -ProjectDir . -HumanFacing` 통과. 룰: `.bat` 한글 0·ASCII 폴더·BOM 없음·CRLF·GUI 진입점(ui.py·index.html) 존재.

## 4. 실행
```
set PYTHONUTF8=1
python -m pytest _개발자료/tests -q -p no:cacheprovider
```
pytest 가 없으면 `python -m pip install pytest`. `unittest discover` 로 돌리면 fixture 모듈이 import 오류로 빠진 채 «통과»처럼 보인다.
